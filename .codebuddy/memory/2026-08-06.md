# 2026-08-06

## ReplyPilot：AI 记忆 → 店铺信息结构化表单

用户反馈「AI 记忆」一添加就报网络/接口错误，根因是自由文本（最多 500 字符）拼进 prompt 后总 token 过长，廉价模型（DeepSeek-V4-Flash）响应超时/截断 → JSON 解析失败。

改动（commit `59f37e5`）：
- `options.html`：删除 AI Memory textarea，改为 4 个短 input：店铺名称(30)/主营品类(50)/发货说明(30)/售后政策(40)
- `options.js`：ids 数组替换 `aiMemory` 为 4 字段；删除 `bindAiMemoryCounter()`
- `utils/storage.js`：`rp_aiMemory` → `rp_storeName/rp_storeCategory/rp_shippingInfo/rp_returnPolicy`
- `utils/parser.js`：新增 `buildStoreContext()`，从 4 字段拼成 ~80 字符结构化英文描述，删除 `MAX_MEMORY`；保留 `MAX_BODY`/`clampText`
- `services/ai-service.js`：两处 prompt 调用传入 4 字段
- `_locales` zh/en：删除 `optAiMemory*` 4 条，新增 `optStoreInfo/optStoreName/optStoreCategory/optShippingInfo/optReturnPolicy` 及 hint/placeholder
- `options.css`：删除 `.rp-char-count` 块
- 旧 storage key `rp_aiMemory` 已无代码引用（旧用户数据保留无害）

## ReplyPilot：店铺信息新增「配送范围」第 5 字段

用户要求跨境店铺也能填支持配送的国家/地区。改动（commit `5a1e759`）：
- `options.html`：售后政策下方新增 `shippingRegions` input（maxlength=60），hint 移到该字段下
- `options.js`：ids 数组加 `'shippingRegions'`
- `utils/storage.js`：默认设置加 `rp_shippingRegions: ''`
- `utils/parser.js`：`buildStoreContext()` 加 `Ships to: <regions>`
- `services/ai-service.js`：generateReply + generateOptions 两处都传 `shippingRegions`
- `_locales` zh/en：新增 `optShippingRegions` / `optShippingRegionsPlaceholder`（en: "e.g. Worldwide / Mainland China only / US, EU, AU"；zh: "如：全球 / 仅限中国大陆 / 支持欧美澳"）
- AI 生成回复时会把配送范围作为店铺上下文告知模型。

## ReplyPilot：设置页新增「问题反馈」链接

用户在设置页底部「打赏支持作者」旁要求加「问题反馈」入口，点击跳邮箱（huangzero2004@gmail.com）。改动（commit `a02ccc4`）：
- `options.html`：`.rp-donate-entry` 内打赏按钮后加分隔点 `·` + `<a id="feedbackLink">`
- `options.css`：新增 `.rp-entry-sep`（灰色分隔点，左右 8px 间距）
- `options.js`：新增 `bindFeedbackLink()`，点击 `preventDefault` 后 `window.open('mailto:huangzero2004@gmail.com?subject=ReplyPilot 问题反馈&body=...')`
- `_locales` zh/en：新增 `feedbackEntryText`（zh「问题反馈」/ en「Feedback」）
- 反馈邮箱统一为 Vercel/网站邮箱 huangzero2004@gmail.com（非 qq 邮箱）

## ReplyPilot：浮窗折叠 + 反馈链接下划线修正

改动（commit `fe6df7a`）：
- 浮窗加折叠按钮：`.rp-card-head-right` 内新增 `collapseBtn`（button，文案 `–`/`+`），点击 `stopPropagation` 后 toggle `.rp-card-collapsed`，隐藏 `.rp-card-body` + `.rp-card-actions`，只留 header 条。按钮在 `reply-ui.js` 的 `createCard()` + `bindCardEvents()` + 返回值里接入；`content.css` 加 `.rp-card-collapse` 样式与 `.rp-card-collapsed` 折叠态。
- 反馈链接下划线修正：用户明确要「默认无下划线、悬浮才出现」（之前方案我理解反了）。`.rp-donate-link` 默认态显式加 `text-decoration: none`，`:hover` 保留 `underline`，覆盖 `<a>` 浏览器默认下划线，确保常驻无下划线。
- 注意：`.rp-donate-link` 同时作用于「打赏支持作者」按钮和「问题反馈」链接（两者共用类）。

## ReplyPilot：版本 1.0.0 → 1.1.0 打包

多轮改动（AI 记忆重构、配送范围、反馈链接、浮窗折叠）累计后升级版本号。
- `manifest.json`：version `1.0.0` → `1.1.0`
- 新增 `.vscodeignore`（排除 `.codebuddy/`、`scripts/`、`*.py`、调试临时文件）
- 打包方式：Chrome 扩展无 `package.json`，用 `Compress-Archive` 打成 zip（PowerShell 5.1 无 `-Exclude` 参数，改用临时移出 `.codebuddy`/`scripts` 再移回的方式排除）
- 产物：`ReplyPilot-1.1.0.zip`（项目内 + 复制到 `D:\迅雷下载\vibe coding\`），已排除工作记忆与调试脚本，59 个条目
- commit `688eee9` 已推送到 master

## ReplyPilot：popup 改为固定 280×280 弹窗

用户要求把 popup 改成固定 280px×280px 正方形弹窗，并删除底部版本信息。改动：
- `popup.css`：`.rp-popup` 加 `height:280px` + `display:flex;flex-direction:column`；header/status 的 `margin-bottom` 14→12px、status `padding` 10→8px；`.rp-actions` 加 `margin-top:auto` 让按钮沉底贴合固定高度；删除 `.rp-popup-footer` 样式块
- `popup.html`：删除 `<footer class="rp-popup-footer">` 整块
- 未改 JS、未改 i18n key（popupVersion 仍保留在 _locales 但不再使用，无害）
- 注意：固定高度后若状态文案变长可能溢出；用 flex column + margin-top:auto 保证按钮区始终沉底
