# 2026-08-18

## ReplyPilot 设置页：测试连接 + 自动保存 (commit 2138c14)
- `options.html`：模型 ID 字段下方新增「测试连接」按钮 + `#testStatus` 状态区。
- `options.js`：
  - 新增 `runConnectionTest()`：读取 apiKey / apiEndpoint，把 `/chat/completions` 推导出 `/models`，GET 请求验证（8s 超时，AbortController）。覆盖 SiliconFlow / OpenAI / custom 通用端点。
  - 新增 `scheduleAutoSave()`：所有输入项 input/change 事件 debounce 500ms 自动 `setMany` 落库；`saveSettings(silent)` 新增 silent 参数显示「已自动保存」。
  - 失败显示红色状态、保存失败显示 `optSaveFailed`。
- i18n：zh_CN / en 各新增 12 key（optAutoSaved / optSaveFailed / optTestConnection / optTesting / optConnectionSuccess / optConnectionFailed / optApiKeyInvalid / optApiKeyMissing / optEndpointMissing / optTimeout / optNetwork 等），两文件 key 已对齐校验通过。
- CSS：`options.css` 新增 `.rp-field-test` / `.rp-test-status` 成功绿失败红样式。
- 注意：Chrome 扩展项目未主动 push（遵循 chrome-ext-no-auto-push 规则）。用户需在 chrome://extensions 点 Reload。

## ReplyPilot Gmail 弹窗「网络错误」根因与修复
- 现象：options 页「测试连接」成功，但 Gmail 弹窗点「生成回复」显示网络错误。
- 根因：options 页 origin 是 `chrome-extension://`，直接 fetch 外部 API 不受页面 CSP 限制；Gmail 弹窗的 chat() 在 content script 内以 `https://mail.google.com/` 运行，Gmail 页面 CSP 的 `connect-src` 拦截了直接 fetch，请求未出浏览器即失败，被包装成「NETWORK_ERROR」。
- 修复：新增 background 消息 `RP_FETCH_CHAT`（`background/service-worker.js` 的 `relayChatRequest`），由 service worker（origin chrome-extension://）转发 `POST /v1/chat/completions`；`services/siliconflow.js` 的 `chat()` 改为 `chrome.runtime.sendMessage({type:'RP_FETCH_CHAT', payload})` 转发，并在回调里按 status 映射 401/404/429/其他错误码。`testConnection()` 保持直接 fetch 不变（仅 options 用）。
- 验证：`node --check` 两文件通过、lint 0 错。
- 注意：Chrome 扩展项目未主动 push；改 background + services 后用户需 Reload 扩展。

## ReplyPilot NVIDIA 404 误判与错误提示布局修复 (commit 766b37a)
- 现象：用户填 endpoint `https://integrate.api.nvidia.com/v1` + model `minimaxai/minimax-m3`，Gmail 弹窗报「模型不存在 (404)」；错误提示挤在 header pill 里影响布局。
- 根因：用户填写的是 OpenAI 兼容 base URL，但 `services/siliconflow.js` 的 `chat()` 直接用该 URL 发 POST，请求实际打到 `https://integrate.api.nvidia.com/v1`（无 `/chat/completions` 路由），NVIDIA 返回 404，代码映射为 MODEL_NOT_FOUND。同时长错误文本放在 `.rp-card-status` pill 里会撑破 header。
- 修复：
  - `services/siliconflow.js` 新增 `normalizeChatEndpoint()`，对缺失 `/chat/completions` 后缀的 endpoint 自动补齐；`chat()` 与 `testConnection()` 均使用 normalize 后的 URL。
  - `content/reply-ui.js` 在卡片 body 顶部新增 `.rp-error-banner`，错误状态时 header 只显示简短 `statusErrorShort`，详细错误显示在 banner 中。
  - `content/content.css` 定义 `.rp-error-banner` 样式（红底、圆角、自动换行）。
  - `_locales/en/messages.json` 与 `zh_CN/messages.json` 新增 `statusErrorShort`。
- 验证：`node --check` JS/JSON 通过、lint 0 错。
- 注意：未主动 push；用户需 Reload 扩展。

## ReplyPilot 三供应商配置不分立根因与修复 (commit 待定)
- 现象：用户在设置页切换 siliconflow / openai / custom 三个模型，API 设置（endpoint/key/model）始终不刷新，三个供应商共用一套。
- 根因：旧架构里存在 legacy flat 字段 `rp_apiKey/rp_apiEndpoint/rp_model`，且 `storage.js#migrateProviderConfigs` 因 `rp_providerConfigs` 在 DEFAULTS 里初始化为 `{}`（永远非空），导致迁移逻辑从来不执行；`resolveProviderConfig` / `readProviderSlot` 在 slot 为空时全部 fallback 到 legacy flat → 三个供应商读同一份全局值。
- 修复（改 5 文件）：
  - `utils/storage.js`：删除 DEFAULTS 里的 legacy flat 三字段；`migrateProviderConfigs` 改为 `normalizeProviderConfigs`，对 siliconflow/openai/custom 三个 slot 全部从各自 PRESET 端点和模型补齐（空 slot 用 preset，不再读 legacy）。
  - `options.js`：`readProviderSlot` / `applyProviderSlot` 只读取该供应商自己的 slot；`saveSettings` 不再写 legacy flat 字段，仅写 `rp_providerConfigs[当前供应商]`。
  - `services/ai-service.js`：`resolveProviderConfig` 仅从该供应商 slot 取，无 legacy 兜底。
  - `popup/popup.js`：`activeKey` 仅从 slot 取。
  - `options.html` + `options.js`：新增「重置供应商配置」按钮（`resetProviders`），清空所有 slot 后由 storage 重新按 preset 播种，解决老数据损坏场景。
  - 两 locale 新增 `optResetProviders` / `optResetProvidersHint` / `optResetDone`（中英文已对齐，值均为 `{message}` 对象）。
- 验证：`node --check` 4 个 JS 通过；locale JSON 值类型 OK；lint 0 错。
- 注意：Chrome 扩展项目未主动 push；改 storage/options/services 后用户必须 Reload 扩展。

## ReplyPilot 回复选项悬浮 tooltip (commit 9841545)
- 按方案 2 实现：每个 `.rp-option-item` 绑定 `mouseenter/mouseleave`，hover 时在鼠标附近弹出固定 `position: fixed` 浮层，展示 `opt.reply` 完整文本。
- `content/reply-ui.js`：新增 `getOptionTooltip()/showOptionTooltip()/hideOptionTooltip()`，浮层作为 `document.body` 下的单例 `.rp-option-tooltip`；定位用 `getBoundingClientRect`（viewport 坐标），上方空间不足时翻到下方，并对右/左边界 clamp。
- `content/content.css`：`.rp-option-tooltip` 设 `max-width:320px`、`max-height:220px`、`overflow-y:auto`、深色底白字、`pointer-events:none` 避免干扰 hover。
- 验证：`node --check` + lint 0 错。未主动 push；用户需在 chrome://extensions 点 Reload。

## ReplyPilot 回复选项 tooltip 被卡片遮挡修复 (commit 3ee0f5a)
- 现象：用户悬浮在回复选项上未看到 tooltip 浮层。
- 根因：`.rp-card` 的 `z-index` 为 `2147483640`，而 `.rp-option-tooltip` 的 `z-index` 为 `2147483600`，tooltip 低于卡片，被卡片自身遮挡。
- 修复：`content/content.css` 将 `.rp-option-tooltip` 的 `z-index` 提升到 `2147483646`（仍低于 32-bit int 上限），确保浮层显示在卡片之上。
- 验证：未主动 push；用户需 Reload 扩展后悬浮即可看到完整回复内容。

## ReplyPilot API 配置项改为 Base URL（对齐硅基流动官方示例，方案 A，commit c1ac172）
- 用户要求：设置页的「API Endpoint（/chat/completions URL）」改为与硅基流动官方示例一致的「Base URL」。
- 改动：
  - `services/siliconflow.js`：`ENDPOINT` 默认从 `.../v1/chat/completions` 改为 `https://api.siliconflow.cn/v1`（保留 `normalizeChatEndpoint()` 自动补 `/chat/completions`，老配置兼容）。
  - `utils/storage.js`：`rp_apiEndpoint` 默认值同样改为 base URL。
  - `options/options.js`：`PROVIDER_PRESETS` 的 siliconflow/openai endpoint 改为 `.../v1`（base URL）。
  - `options/options.html`：字段标签改「API Base URL」，hint + 教程步骤 2 文案改为 base URL 说法。
  - `_locales/en` + `_locales/zh_CN`：`optApiEndpoint`/`optApiEndpointPlaceholder`/`optApiEndpointHint`/`optEndpointMissing`/`optTutorialStep2Title`/`optTutorialStep2` 全部改指 base URL（zh：接口地址→基础地址）。
  - `README.md`：填写配置段「API Endpoint」改为「API Base URL」。
- 未主动 push；用户需在 chrome://extensions 点 Reload 生效。

## ReplyPilot 按用户反馈重新生成回复（已落地，commit 9869e70）
- 用户要求：生成结果不满意时，支持手动输入修改意见并按意见重新生成。用户回「1」确认执行。
- 已落地：
  - `content/reply-ui.js`：卡片底部新增可折叠「修改意见」面板（textarea +「按意见重新生成」按钮 + 错误提示）。原「重新生成」保留为无反馈兜底。actions 行增加「修改意见」链接按钮（展开/收起）。
  - 状态缓存：`rememberContext()` 记录最近一次邮件上下文与当前回复；选择某方案时同步更新 `currentReply`；切换邮件/清空时 `clearContext()` 重置，避免串信。
  - `utils/parser.js`：新增 `buildRefinementPrompt`，融合原文 + 当前回复 + 用户意见，要求模型按意见改写并返回 `{"reply": "..."}`。
  - `services/ai-service.js`：新增 `refineReply`，复用同一 `RP.siliconflow.chat` 接口与解析逻辑。
  - i18n：zh_CN/en 新增 `feedbackToggle` / `feedbackPlaceholder` / `regenerateWithFeedback` / `feedbackNoReply` / `statusRefining` 五个 key，两文件已校验。
  - `content/content.css`：新增 `.rp-card-feedback` / `.rp-card-feedback-input` / `.rp-card-feedback-err` / `.rp-btn-link` 样式，卡片 360px 不撑破。
- 验证：`node --check` 三 JS 文件通过，lint 0 错，locale JSON 解析通过。未主动 push；用户需 Reload 扩展生效。

## ReplyPilot 加请求冷却 + 429 Retry-After 等待提示 (commit 95cb4af)
- 背景：用户用 NVIDIA 免费接口（40 rpm 共享额度）频繁触发 429，原错误只说「请稍后再试」看不到要等多久。
- 改动：
  - `background/service-worker.js` `relayChatRequest` 的 resolve 对象新增 `retryAfter`（读 `res.headers.get('Retry-After')`）。
  - `services/siliconflow.js` 429 分支把 `Retry-After` 解析为整数挂到 error 的 `retryAfter`。
  - `content/reply-ui.js`：`friendlyError()` 在 RATE_LIMITED 且 `e.retryAfter` 存在时拼接「约需等待 N 秒」；`onGenerate()` 新增 `COOLDOWN_MS=1500` 冷却——请求 settle 后按钮继续禁用 1.5s，冷却期内点击直接 return，防止手快连点二次触发限流。
  - i18n：zh_CN/en 新增 `errRateLimitedWait`（含 `{{secs}}` 占位，i18n.js 已支持 `{{var}}` 插值）。
- 验证：`node --check` 三 JS + locale JSON 解析均通过。未主动 push；用户需 Reload 扩展生效。

## ReplyPilot 每个引擎独立保存配置（per-provider config slots，commit 5229364）
- 用户痛点：切换 provider 时 apiKey/endpoint/model 不保留，每次切回 custom 都看到旧值要重填。
- 根因：原结构扁平存 `rp_apiEndpoint/rp_apiKey/rp_model` 各一份，切 provider 只覆盖成新预设，旧 provider 配置丢失。
- 改动：
  - `utils/storage.js`：DEFAULTS 新增 `rp_providerConfigs: {}`；`getAll()` 调用 `migrateProviderConfigs()` 自动把现有扁平字段按当前 `rp_provider` 搬进 `rp_providerConfigs[provider]`（老用户升级不丢配置，幂等）。
  - `services/ai-service.js`：新增 `resolveProviderConfig(s)` 从 `s.rp_providerConfigs[s.rp_provider]` 取 endpoint/key/model，slot 为空时回退扁平字段；generateReply/generateOptions/refineReply 三处 chat 调用改用 `cfg.*`。
  - `options/options.js`：重写 `loadSettings`（按当前 provider 的 slot 填表）、`applyProviderSlot`（切 provider 时先存旧 slot 再读新 slot，无 slot 则回落预设）、`saveSettings`（把当前输入写入 `rp_providerConfigs[currentProvider]`，同时同步扁平字段兼容）；provider change 事件先存旧 slot 再切新 slot。
  - `popup/popup.js`：状态徽章按当前 provider 的 slot key 判断已配置。
- 效果：siliconflow / openai / custom 三套配置互不覆盖，切回即显示各自保存的 key/地址/模型。
- 验证：`node --check` 四 JS 全通过、lint 0 错。未主动 push；用户需 Reload 扩展生效。

## ReplyPilot 切回自定义引擎配置被清空修复
- 现象：每次切到「自定义 (OpenAI 兼容)」时，Base URL / API Key / 模型 ID 被刷新成空，不保留上次填写的记录。
- 根因：`options/options.js` 的 `saveSettings()` 里写 `obj.rp_providerConfigs = {}`，每次保存都会把整个对象替换掉，只保留当前 provider 的 slot；切到其它 provider 时，旧 provider 的 slot 被整体覆盖丢失，再切回来只能回落空预设。
- 修复：
  - `saveSettings(silent, baseSettings)` 改为先读取（或接收传入的）现有 `rp_providerConfigs`，只覆盖当前 provider 的 slot，保留其它 provider 的 slot。
  - provider `<select>` 的 change 事件把内存中已合并的 `settings` 传给 `saveSettings(true, settings)`，确保切换前刚改但未 autosave 的旧 provider 值也被保留。
- 验证：`node --check options/options.js` 通过，lint 0 错。
- 注意：未主动 push；用户需在 chrome://extensions 点 Reload 扩展生效。

## ReplyPilot API Key 已配置却提示未配置修复 + 去掉重置按钮 (commit 273498d)
- 现象：options 页显示已配置 SiliconFlow 的 Base URL / API Key / Model，测试连接成功；但 Gmail 弹窗生成回复时仍报「错误：未配置 API Key，请打开设置添加。」
- 根因：`services/ai-service.js#checkApiKey` 仍在检查旧的扁平字段 `s.rp_apiKey`，而 per-provider 改造后配置实际存在 `s.rp_providerConfigs[provider].apiKey`，两者不一致导致 `API_KEY_MISSING` 被误抛。
- 修复：
  - `services/ai-service.js`：`checkApiKey` 改为 `var cfg = resolveProviderConfig(s);` 后检查 `cfg.apiKey`。
  - 去掉重置供应商配置按钮与提示：`options/options.html` 删除 `resetProviders` 按钮和 `optResetProvidersHint`；`options/options.js` 删除 `bindResetProviders()` 调用与函数定义；`_locales/en/messages.json` 与 `_locales/zh_CN/messages.json` 撤销本轮新增的三个 reset key（`optResetProviders` / `optResetProvidersHint` / `optResetDone`）。
- 验证：`node --check` services/ai-service.js、options/options.js、popup/popup.js、utils/storage.js 均通过；locale JSON 解析通过；lint 0 错。
- 注意：Chrome 扩展项目未主动 push；用户需在 chrome://extensions 点 Reload 扩展生效。

## ReplyPilot 移除「修改意见」功能 + 修复 lastContext 错误 (commit 4483ca6)
- 用户要求去掉整个「修改意见（refine）」功能，并报告「已显示结果却还显示错误」的不同步问题。
- 根因：`reply-ui.js` 的 `lastContext` / `currentReply` 未用 `var/let/const` 声明；严格模式下 `rememberContext()` 给未声明变量赋值抛出 `ReferenceError: lastContext is not defined`，被 `.catch` 捕获后显示红条，所以先看到结果、后弹错误（结果和错误不同步）。
- 改动（7 文件）：
  - `content/reply-ui.js`：删除 feedback 面板 DOM、feedbackToggle/feedbackRegen 事件、rememberContext/clearContext/onRefine 函数、cardRefs 中 feedback 字段、refreshTexts 的 feedback 文案刷新、onClear 中 feedback 清理、renderOptions 的 currentReply 赋值；onGenerate 去掉 rememberContext 调用。
  - `services/ai-service.js`：删除 refineReply 函数及导出。
  - `utils/parser.js`：删除 buildRefinementPrompt 函数及导出。
  - `content/content.css`：删除 .rp-card-feedback* 与 .rp-btn-link* 样式。
  - `zh_CN/en`：删除 feedbackToggle / feedbackPlaceholder / regenerateWithFeedback / feedbackNoReply / statusRefining 五个 key。
- 验证：node --check 三 JS 通过；grep 确认无 feedback/refine/lastContext/currentReply 残留（仅 options 页「Feedback」邮件链接与截图 mockup 文案属无关）。未主动 push；用户需 Reload 扩展生效。

## ReplyPilot 重新实现「按意见重新生成回复」(commit e350b21)
- 用户要求：生成结果不满意时，在意见输入框写反馈，重新生成回复。用户回「1 先 git 再改」确认。
- 方案 B（独立意见输入框，常驻不破坏原草稿）：卡片 textarea 与 options 面板之间新增 `.rp-card-revise`（`input.rp-card-revise-input` + 按钮 `reviseReply`），有回复后显示，支持 Enter 提交 / Shift+Enter 换行，空意见提示 errNoInstruction。
- 防历史踩坑（4483ca6 删除过 refine 功能，根因是 lastContext/currentReply 未 var 声明导致严格模式 ReferenceError）：本轮用 `var lastContext=null; var lastReply='';` 显式声明，`refilledReply(reply, ctx)` 统一保存 ctx 与 lastReply，onGenerate/renderOptions 的 choose 都经过 refilledReply 写入 lastContext，onClear/resetCard 清空。
- 改动文件：content/reply-ui.js（UI+onRevise+状态）、content/content.css（.rp-card-revise* 样式）、services/ai-service.js（reviseReply 复用 chat+parseReply）、utils/parser.js（buildRevisePrompt）、_locales/zh_CN+en（reviseReply/revisePlaceholder/statusRevising/statusRevised/errNoInstruction 五键，已对齐校验）。
- 验证：node --check 三 JS 通过；locale JSON 解析、key 对齐、值类型 object 均 OK；lint 0 错。未主动 push；用户需在 chrome://extensions 点 Reload 扩展生效。

## ReplyPilot 修复 Gmail 无法读取邮件正文 (commit 1ee6d7b)
- 现象：Gmail 会话视图已打开邮件，点击「生成回复」仍红条提示「无法读取邮件内容，请确认邮件已打开。」
- 根因：`content/gmail-dom.js#getEmailBody()` 只按固定选择器（`.a3s.aiL` 等）平铺匹配，再取 `candidates[candidates.length - 1]`。Gmail 新版会话视图中同一页可能有多个 `.a3s`，最后一个不一定是最新邮件正文；且当最新邮件 listitem 结构变化时，固定选择器可能根本匹配不到。
- 修复：新增策略 1——遍历所有 `[role="listitem"]`，从最后一个可见项向上找，先确认它含邮件元信息（`.gD` / `data-message-id` / `.a3s`），再在其内部按 `.a3s.aiL → .a3s → .ii.gt .a3s → .ii.gt` 顺序提取正文；策略 2 保留原选择器作为兜底，并改为从后向前跳过空结果，失败时返回 `BODY_EMPTY`。同时避免读取回复框草稿（回复框在 listitem 外）。
- 验证：`node --check content/gmail-dom.js` 通过。未主动 push；用户需在 chrome://extensions 点 Reload 扩展生效。
